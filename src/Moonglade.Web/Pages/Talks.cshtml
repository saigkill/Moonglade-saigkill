@page "/pages/talks"
@using Moonglade.Data.Entities
@model Moonglade.Web.Pages.TalksModel
@{
}
@section scripts {
    <script src="~/lib/syncfusion/ej2/dist/ej2.min.js" type="text/javascript"></script>
    <script src="~/js/app/apply-syncfusion.js" type="text/javascript"></script>
    <script src="https://cdn.syncfusion.com/ej2/syncfusion-helper.js" type="text/javascript"></script>
}
<div class="row">
    <h1>@Model.Intro.Value</h1>
    <div>
        <img src="https://moongladesm.blob.core.windows.net/images/me/talks.jpg" alt="openSUSE Conference" />
    </div>
    <h2 style="margin-top: 1em;">@Model.Slides.Value:</h2>
    <div class="control_wrapper accordion-control-section">
        <ejs-accordion id="defaultAccordion">
            <e-content-template>
                @foreach (var t in Model.Talks)
                {
                    <div class="e-card" id="@t.Id">
                        <div class="card-header">
                            <div class="e-card-header-caption">
                                <div class="e-card-header-title font-gray"><h4 class="font-gray">@t.Title</h4></div>
                            </div>
                        </div>
                        <div class="e-card-content">
                            <div class="card-text font-gray"><small>@Model.Held.Value: @t.Date.ToString("D") on @t.Where</small></div>
                            <p class="card-text font-gray">@t.Summary</p>
                            <div class="" id="talk-@t.Id">
                                <iframe src="@t.Link" width="595" height="485" title="@t.Title" style="border:1px solid #CCC; border-width:1px; margin-bottom:5px; max-width: 100%;" allowfullscreen> </iframe><div style="margin-bottom:5px"> <strong> <a href="@t.Link" title="@t.Title" target="_blank">@t.Title</a> </strong> von <strong><a href="https://www.slideshare.net/saigkill" target="_blank" rel="noopener">Sascha Manns</a></strong> </div>
                            </div>
                        </div>
                    </div>
                }
            </e-content-template>
        </ejs-accordion>
    </div>
</div>